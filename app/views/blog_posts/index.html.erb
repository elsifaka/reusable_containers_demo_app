<% if public_view? %>
  Avé

  Il y a actuellement <%= @blog_posts.count %>.

  <ul>
  <% @blog_posts.each do |b| %>
    <li><%= link_to b.entry.title, b.entry.permalink %>
  <% end %>
<% else %>

<h1>Listing blog_posts</h1>

<table>
  <tr>
    <th>Name</th>
    <th>Title</th>
    <th>Content</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @blog_posts.each do |blog_post| %>
  <tr>
    <td><%= blog_post.name %></td>
    <td><%= blog_post.title %></td>
    <td><%= blog_post.content %></td>
    <td><%= link_to 'Show', blog_post %></td>
    <td><%= link_to 'Edit', edit_blog_post_path(blog_post) %></td>
    <td><%= link_to 'Destroy', blog_post, confirm: 'Are you sure?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Blog post', new_blog_post_path %>
<% end %>
